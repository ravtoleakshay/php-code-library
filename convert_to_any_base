<?php

$intNum = 5;
$intBase = 2;

function convertNum( $intNum, $intBase ) {
	if( $intBase >= 2 && $intBase <= 10 ) {
		return base_convert( $intNum, 10, $intBase );
	} else {
		return 'Base, Out of range';
	}
}

echo convertNum( $intNum, $intBase );
